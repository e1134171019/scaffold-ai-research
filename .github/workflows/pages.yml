name: Deploy static site to GitHub Pages

on:
  push:
    branches: ["main"]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: "pages"
  cancel-in-progress: true

jobs:
  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Build v32 website with revised Scheme 1 appendix
        run: python build/build_v26.py

      - name: Verify generated narrative assets
        run: |
          test -f _site/index.html
          test -f _site/narrative-v23.js
          test -f _site/feature-bridge-v24.js
          test -f _site/act3-v29.js
          test -f _site/act3-v29.css
          test -f _site/act4-v30.js
          test -f _site/act4-v30.css
          test -f _site/act5-v31.js
          test -f _site/act5-v31.css
          test -f _site/scheme1-devpath-v32.js
          test -f _site/scheme1-devpath-v32.css
          grep -q "narrative-v23.js?v=32" _site/index.html
          grep -q "act4-v30.js?v=32" _site/index.html
          grep -q "act5-v31.js?v=32" _site/index.html
          grep -q "scheme1-devpath-v32.js?v=32" _site/index.html
          grep -q "scheme1-devpath-v32.css?v=32" _site/index.html

      - name: Configure Pages
        uses: actions/configure-pages@v5

      - name: Upload static site
        uses: actions/upload-pages-artifact@v3
        with:
          path: "_site"

      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
